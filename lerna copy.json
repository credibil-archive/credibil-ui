{
  "packages": [
    "packages/*"
  ],
  "version": "0.0.27",
  "command": {
    "publish": {
      "verifyAccess": false,
      "conventionalCommits": true,
      "createRelease": "github",
      "fromGit": true
    }
  }
}

{
    "version": "lerna version --no-changelog --no-push --no-git-tag-version",
    "build": "lerna run --parallel --scope \"@credibil/*\" build",
    "publish": "lerna publish",
}